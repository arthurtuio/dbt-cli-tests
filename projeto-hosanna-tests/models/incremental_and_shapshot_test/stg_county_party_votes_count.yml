version: 2

models:
  - name: stg_county_party_votes_count
    description: contagem de votos agrupados por county e party
    columns:
      - name: county
      - name: party
      - name: total_votes
        description: sum(votes)
